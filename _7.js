"use strict";
// Write a JavaScript function to check whether a string is blank or not.

const str = prompt("check whether a string is blank or not");
const checkStringIsBlank =(str) => {
return  str.length === 0 ? str.length === 0 : str.length === 0;
}
document.getElementById("string").innerHTML = checkStringIsBlank(str);